
<div ng-controller='listItems'
<table class='center'>

  <tbody>
      <tr>
      <tr ng-repeat="item in items">	
        <td class='mobile'><big>{{ item.title }}<big></td>
        <td><%= button_to 'Done', controller: 'items', action: 'done', id: item, class: 'red-button' %></td>
      </tr>
    <% end %>
      <tr><td><form><input type='button' value="Refresh" onClick="window.location.reload()"</form> </td></td></tr>

  </tbody>
</table>
